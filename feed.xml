<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://garyzhang1006.github.io/nu-intro/feed.xml" rel="self" type="application/atom+xml"/><link href="https://garyzhang1006.github.io/nu-intro/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-22T20:34:46+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Gary Zhang — student researcher working on machine learning for physics simulation. </subtitle><entry><title type="html">Building EcoRise: a hardened AI leaderboard for climate action</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/building-ecorise/" rel="alternate" type="text/html" title="Building EcoRise: a hardened AI leaderboard for climate action"/><published>2026-06-15T10:00:00+00:00</published><updated>2026-06-15T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/building-ecorise</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/building-ecorise/"><![CDATA[<p>EcoRise started as a simple idea I describe to friends as “Kahoot for the planet”: a leaderboard where students log real environmental actions and schools compete on climate impact. The hard part was never the leaderboard UI. It was making the points trustworthy. Once points rank people publicly, points effectively become money, and anything that looks like money attracts cheating, races, and edge cases. Most of my engineering time went into hardening, not features.</p> <h2 id="treating-points-like-money">Treating points like money</h2> <p>The core of the backend is a transactional, idempotent <code class="language-plaintext highlighter-rouge">point_events</code> ledger. Every award is an entry, idempotency keys stop double-counting, and the ledger is the single source of truth instead of a mutable score column. Around that I added membership authorization so you can only act inside your own leaderboard, anti-cheat quests, and foreign keys plus indexes so the database itself enforces the invariants. One bug I am glad I caught was a race in the leaderboard that could surface fake or partially written data; fixing it meant making the read path consistent with the ledger rather than trusting a cached aggregate.</p> <p>The stack is React 19 with Vite on the front, Node and Express with SQLite (better-sqlite3) on the back. Auth is a JWT in an httpOnly cookie with CSRF protection. I wrote 20 integration tests that exercise the awarding and authorization paths, because those are exactly the places where a subtle mistake hands someone free points.</p> <h2 id="the-vision-model-and-a-real-trained-cnn">The vision model and a real trained CNN</h2> <p>Logging an action needs evidence, so EcoRise verifies submitted photos. I use a Claude vision model for general eco-image understanding, but I did not want to lean entirely on a hosted model for the most common case, sorting trash. So I trained an ONNX CNN on a dataset of 3527 images. It reaches precision 0.97 with a false positive rate of 0.07. The false positive rate mattered more to me than raw accuracy, because a model that wrongly approves bad submissions is a model that lets people farm points.</p> <h2 id="what-the-adversarial-reviews-taught-me">What the adversarial reviews taught me</h2> <p>The single most useful process change was running three adversarial code reviews against the project using a council of GPT-5.5 reviewers. They were instructed to attack the system, and they found assumptions I had stopped questioning, mostly around authorization gaps and AI gating. That loop is why the hardening went as deep as it did.</p> <p>Two honest caveats remain. The repository main history still contains an old committed <code class="language-plaintext highlighter-rouge">.env</code> and <code class="language-plaintext highlighter-rouge">node_modules</code>, so any real keys need rotation before this is treated as production. And the live demo runs in demo mode with guest auto-login, which is convenient for showing the idea but is not the security posture I would ship to a real classroom.</p> <p>Building EcoRise reset my intuition about what “AI app” means. The model work was maybe a fifth of the effort. The rest was making a system where a number on a screen actually deserves to be believed.</p> <p>Code: <a href="https://github.com/wwhaiming/EcoRise">github.com/wwhaiming/EcoRise</a></p>]]></content><author><name></name></author><category term="projects"/><category term="llms"/><category term="product"/><category term="evaluation"/><category term="privacy"/><summary type="html"><![CDATA[engineering an eval-driven, privacy-first AI product students can actually trust]]></summary></entry><entry><title type="html">GreenPath: grounding LLM immigration guidance with JSON contracts</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/grounding-llm-guidance-greenpath/" rel="alternate" type="text/html" title="GreenPath: grounding LLM immigration guidance with JSON contracts"/><published>2026-06-13T10:00:00+00:00</published><updated>2026-06-13T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/grounding-llm-guidance-greenpath</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/grounding-llm-guidance-greenpath/"><![CDATA[<p>When I started GreenPath, a web app that helps people navigate the U.S. green card process, the obvious failure mode was clear from the first prototype. An LLM will happily produce confident, fluent text about immigration pathways, and some of it will be wrong. For a domain where a wrong priority date or a misread eligibility rule has real consequences, “sounds right” is not good enough. The whole project became an exercise in fencing the model in.</p> <h2 id="four-assistants-each-with-a-json-contract">Four assistants, each with a JSON contract</h2> <p>GreenPath is not one chatbot. It is four narrow assistants, each backed by a system prompt that ends in a strict JSON contract: a pathway advisor that takes intake answers and returns the most likely green card pathway, a stage-by-stage Q&amp;A guide, a document review that checks forms for errors before submission, and an interview simulator that runs a USCIS-style practice interview.</p> <p>The contract matters more than the prose around it. Instead of letting the model free-write, each prompt forces the output into a fixed JSON shape that the front end knows how to parse and render. That gives me a seam I can test. A free-text answer is hard to assert against. A JSON object with named fields is something I can validate, diff, and catch when it drifts. It also lets the UI stay dumb and stable: the HTML parsers expect one shape, so I can swap the model underneath without touching them.</p> <p>That swap happened more than once. The backend went from Anthropic to Groq’s Llama to OpenAI’s gpt-4o-mini over the project’s life, mostly for cost reasons. Because every provider’s response gets normalized back to the same <code class="language-plaintext highlighter-rouge">{content:[{type:text}]}</code> envelope before parsing, none of those moves required rewriting the feature code. The contract was the stable interface; the model was a swappable detail.</p> <h2 id="grounding-and-keeping-the-key-off-the-client">Grounding, and keeping the key off the client</h2> <p>A JSON contract stops malformed output, but it does not stop confident hallucination. The second layer is grounding. Immigration answers route through a proxy that injects real reference data, so questions about priority dates lean on the actual Visa Bulletin rather than the model’s memory. A query like “EB-2 India” comes back anchored to a real wait estimate from that data, not an invented one. Across the four features I keep one rule absolute: these are guidance tools, never legal advice, and every answer points back to USCIS.gov or an accredited attorney.</p> <p>The hardest lessons were not about prompting at all. Early builds called the LLM directly from the browser with the API key embedded in the page, which is convenient and completely wrong. Moving to a serverless proxy that holds the key server-side fixed the exposure but added its own traps, like a stale duplicate file hijacking which build got served. And a key once committed to git history stays compromised no matter how clean the current tree looks; rotation is the only fix.</p> <p>To check the contracts actually hold, I wrote an eval harness that imports the production prompts and runs them live against the model. The point was never a vanity score. It was a regression net, so that the next time I change a prompt or switch providers, I find out immediately if the JSON stops parsing.</p> <p>GreenPath was built for the USAII Global AI Hackathon. Source: <a href="https://github.com/wwhaiming/greenpath">github.com/wwhaiming/greenpath</a></p>]]></content><author><name></name></author><category term="projects"/><category term="llms"/><category term="product"/><category term="json-schema"/><summary type="html"><![CDATA[strict JSON-contract AI features layered over official government sources]]></summary></entry><entry><title type="html">Predicting aneurysm blood flow with learned surrogates</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/neural-surrogates-for-aneurysm-hemodynamics/" rel="alternate" type="text/html" title="Predicting aneurysm blood flow with learned surrogates"/><published>2026-06-02T10:00:00+00:00</published><updated>2026-06-02T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/neural-surrogates-for-aneurysm-hemodynamics</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/neural-surrogates-for-aneurysm-hemodynamics/"><![CDATA[<p>For the last few months I have been working in a lab at UPenn on predicting blood flow inside aneurysms. The goal is a learned surrogate: instead of running a full CFD simulation for every new vessel geometry and inflow waveform, train models that produce the velocity field directly. This post is about the 2D version of the problem, where I have spent most of my time trying to understand the architecture and fix its training.</p> <h2 id="decomposing-the-flow">Decomposing the flow</h2> <p>The core idea is not to predict the raw velocity field node by node and timestep by timestep. That is a lot of correlated output. Instead, for each trajectory the velocity field is decomposed into three pieces:</p> <p><code class="language-plaintext highlighter-rouge">U(x, t) = u_mean(x) + V(x) @ A(t)^T</code></p> <p>Here <code class="language-plaintext highlighter-rouge">u_mean(x)</code> is the time-mean velocity at each mesh node, <code class="language-plaintext highlighter-rouge">V(x)</code> is a small per-node POD mode basis (we keep the top 4 modes), and <code class="language-plaintext highlighter-rouge">A(t)</code> is the set of time-varying coefficients that turn those static modes into a moving flow. Classical POD gets these from an SVD of the simulated field. The surrogate replaces each piece with a learned model so it can generalize to geometries it never simulated.</p> <p>So there are three models. A geometry-aware spatial neural operator predicts the mode basis <code class="language-plaintext highlighter-rouge">V</code> per node. A recurrent model (a coefficient LSTM) predicts the time coefficients <code class="language-plaintext highlighter-rouge">A</code> from the inflow waveform. And a MeshGraphNet-style operator predicts the time-mean field <code class="language-plaintext highlighter-rouge">u_mean</code>, learning a residual over a blurred prior rather than the field from scratch.</p> <h2 id="what-the-geometry-forces-you-to-do">What the geometry forces you to do</h2> <p>The thing that surprised me is how much of the design is about the sac. A vessel is mostly tube-like fluid, but the aneurysm sac is a small fraction of the nodes where the interesting recirculation happens, so a plain mean-squared loss just ignores it. The pipeline carries signed distance features (distance to the wall, signed distance to the sac neck) and splits nodes into vessel, neck, and sac regions, weighting the loss by inverse frequency so the rare sac nodes actually count. The conditioning is sac-aware too: it pools features separately over the whole graph and over sac-only nodes.</p> <h2 id="the-part-that-was-hard">The part that was hard</h2> <p>My main assignment was the training curves, which looked off. The mean model showed roughly a 2.75x train-to-validation gap with a validation loss that climbed while training loss kept dropping, and the coefficient model plateaued in validation long before training did. Textbook overfitting. The fixes were unglamorous and incremental: cut model capacity (hidden width 256 to 128, fewer layers), raise regularization (more dropout, larger weight decay), and lean harder on the retrieval prior so the mean model has less to invent. That narrowed the gap. The sac region is still the hardest part of the field, and data augmentation is the next lever I want to try.</p> <p>Code: <a href="https://github.com/rmallela26/aorta_modeling">rmallela26/aorta_modeling</a></p>]]></content><author><name></name></author><category term="research"/><category term="machine-learning"/><category term="physics"/><category term="cfd"/><category term="biomedical"/><summary type="html"><![CDATA[what I am working on in the UPenn lab: 2D/3D hemodynamics surrogates on vessel meshes]]></summary></entry><entry><title type="html">Learning mesh-based physics simulation with graph neural networks</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/learning-mesh-based-physics-simulation/" rel="alternate" type="text/html" title="Learning mesh-based physics simulation with graph neural networks"/><published>2026-05-20T10:00:00+00:00</published><updated>2026-05-20T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/learning-mesh-based-physics-simulation</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/learning-mesh-based-physics-simulation/"><![CDATA[<p>I have been spending time with mesh-based physics simulation, specifically the line of work that started with DeepMind’s MeshGraphNets. The idea is appealing: instead of running a slow numerical solver on every time step, you train a graph neural network to predict the next state of a simulation mesh directly. I worked mostly on top of the open-source <a href="https://github.com/DonsetPG/graph-physics">DonsetPG/graph-physics</a> framework, which reimplements that approach in PyTorch, and I want to write down what the method actually looks like and where I got stuck.</p> <h2 id="how-the-model-is-set-up">How the model is set up</h2> <p>The mesh is the graph. Each node carries a small feature vector (for the CylinderFlow case, two velocity components plus a node-type label), and each edge carries relative geometry: the displacement between its two nodes and the distance, so three edge features. The model is an encode-process-decode network. An encoder lifts node and edge features into a hidden space (size 32 in the config I used), then a fixed number of message-passing layers (5 here) update node and edge states by passing information along edges, and a decoder reads out the predicted change in velocity for the next step. The node-type label matters more than I expected: inflow, outflow, wall, and obstacle nodes are treated differently, and the loss is masked so the model is only graded on the node types it should actually be predicting.</p> <p>The detail that took me longest to appreciate is training noise. The model predicts one step at a time but is used autoregressively to roll out long trajectories, so small errors compound. Adding Gaussian noise to the inputs during training (sigma around 0.02) forces the network to learn to correct itself, which is what keeps a rollout from drifting into nonsense after a few dozen steps. It is a simple trick that does a lot of work.</p> <h2 id="trying-an-adversarial-variant">Trying an adversarial variant</h2> <p>The supervised one-step loss tends to produce predictions that are smooth but slightly washed out, losing fine structure in the wake behind the cylinder. I forked the framework to test whether an adversarial term would help. The simulator becomes a generator, and I added a per-node discriminator built from the same message-passing blocks, conditioned on the same input graph. Because the discriminator outputs a logit per node rather than one score for the whole mesh, it is essentially a patch-GAN transposed onto a mesh, so the adversarial gradient localizes to regions that look unphysical. Training uses manual optimization in Lightning with two separate optimizers.</p> <p>I want to be honest about the limits here. I do not have a GPU or the full dataset locally, so the adversarial variant is unit-tested but not yet trained at scale, and I cannot claim it improves rollout accuracy. The fork is a working hypothesis, not a result.</p> <h2 id="a-visualization-to-build-intuition">A visualization to build intuition</h2> <p>To understand message passing rather than just run it, I also built a single-file interactive page that draws a real Delaunay mesh, animates information propagating across edges, and lets you inspect node features and compare fields. The physics there is an analytic emulation of a Karman wake, not a trained model, so it is a teaching tool, not an evaluation.</p> <p>Fork: <a href="https://github.com/garyzhang1006/graph-physics-gan">github.com/garyzhang1006/graph-physics-gan</a></p>]]></content><author><name></name></author><category term="research"/><category term="machine-learning"/><category term="physics"/><category term="graph-neural-networks"/><category term="simulation"/><summary type="html"><![CDATA[notes on graph network simulators for fluid dynamics on unstructured meshes]]></summary></entry><entry><title type="html">Solu: predicting molecular solubility in the browser</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/predicting-solubility-in-the-browser/" rel="alternate" type="text/html" title="Solu: predicting molecular solubility in the browser"/><published>2026-05-10T10:00:00+00:00</published><updated>2026-05-10T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/predicting-solubility-in-the-browser</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/predicting-solubility-in-the-browser/"><![CDATA[<p>I built a small tool called Solu for a hackathon (DSH Hacks V1, theme AI x STEM education). It predicts a molecule’s water solubility from its structure and then tries to explain why, and the prediction runs entirely in the browser. No server, no API call, no large language model in the prediction path. You type or pick a molecule and it answers locally.</p> <h2 id="how-it-works">How it works</h2> <p>The pipeline is short. You enter a SMILES string (or pick one of 14 examples), RDKit-JS compiled to WebAssembly draws the 2D structure and computes nine interpretable descriptors: Crippen LogP, molecular weight, topological polar surface area, hydrogen-bond donors and acceptors, rotatable bonds, aromatic rings, fraction of sp3 carbon, and ring count. Those nine numbers feed a gradient-boosting model trained in Python with scikit-learn, exported to ONNX, and run client-side through ONNX Runtime Web. The model is deterministic, so the same molecule always gives the same answer.</p> <p>I trained on the Delaney/ESOL dataset, a published set of 1,128 measured aqueous solubilities. On a held-out 20% test set the shipped gradient-boosting model gets RMSE 0.75 log units and R2 0.88, compared to RMSE 1.04 for the classic linear ESOL equation on the same split. That improvement from 1.04 to 0.75 is the one number I actually care about, because it is measured on data the model never saw.</p> <p>There is also a “why” panel. A separate interpretable linear surrogate breaks the prediction into per-property contributions, so you can see that, say, high LogP pushed a molecule toward insoluble. The goal was teaching, not just a number.</p> <h2 id="the-part-that-was-actually-hard">The part that was actually hard</h2> <p>The model trains in Python but runs in the browser, and those are two different RDKit builds. If the two builds compute a descriptor even slightly differently, the browser prediction drifts away from the trained model and nobody notices. This is exactly what bit me: the definition of the hydrogen-bond-acceptor count changed between RDKit versions. The fix was unglamorous but necessary, pinning Python rdkit==2025.3.4 to match RDKit-JS 2025.03.4.</p> <p>To keep it honest I added parity gates. One checks RDKit-JS descriptors against RDKit-Python to 1e-5 (rounding only). Another checks ONNX output against scikit-learn. A third runs the full browser chain, RDKit-JS into ONNX Runtime Web, and confirms it reproduces the Python prediction bit-for-bit, diff 0.0. With those gates, browser predictions are provably identical to the trained model rather than approximately identical.</p> <h2 id="what-i-learned">What I learned</h2> <p>The interesting engineering was not the model, it was making “train here, run there” trustworthy. A gradient-boosting model on nine descriptors is unremarkable; the descriptors agreeing across two language ecosystems is where the bugs hide. There is also an optional tutor card (Claude Sonnet behind a small local proxy) that explains a prediction using structurally nearby real measured molecules, but I kept it strictly optional so the core tool stays fully offline and deterministic.</p> <p>Code: <a href="https://github.com/garyzhang1006/solu">github.com/garyzhang1006/solu</a></p>]]></content><author><name></name></author><category term="projects"/><category term="machine-learning"/><category term="chemistry"/><category term="onnx"/><category term="web"/><summary type="html"><![CDATA[RDKit-JS plus ONNX for fully client-side cheminformatics ML]]></summary></entry><entry><title type="html">Exos: classifying exoplanets from Kepler and TESS data</title><link href="https://garyzhang1006.github.io/nu-intro/blog/2026/classifying-exoplanets/" rel="alternate" type="text/html" title="Exos: classifying exoplanets from Kepler and TESS data"/><published>2026-04-28T10:00:00+00:00</published><updated>2026-04-28T10:00:00+00:00</updated><id>https://garyzhang1006.github.io/nu-intro/blog/2026/classifying-exoplanets</id><content type="html" xml:base="https://garyzhang1006.github.io/nu-intro/blog/2026/classifying-exoplanets/"><![CDATA[<p>When NASA’s Kepler telescope watched a star dim slightly and repeatedly, it could mean a planet crossing in front of it, or it could be two stars eclipsing each other, instrument noise, or a quirk in the data. Telling a real transit from a false alarm is a genuine triage problem that astronomers spend real effort on. I built Exos, a small machine learning model that does that triage in the browser, on real NASA data, and pairs it with an AI tutor that explains the reasoning.</p> <h2 id="the-data-and-the-leakage-trap">The data and the leakage trap</h2> <p>I trained on two NASA Exoplanet Archive catalogs: the Kepler cumulative KOI table and the TESS TOI table. Combined, that is 17,492 objects of interest, of which 10,253 are labeled CONFIRMED or FALSE POSITIVE (4,070 planets, 6,183 false positives). The still-unconfirmed candidates are held out as “unknowns” you can test.</p> <p>The biggest mistake I had to avoid was label leakage. The KOI table ships with NASA’s own false-positive flags, which encode the human verdict. A model that sees those columns just memorizes the answer and learns no physics. So I dropped them and trained only on 11 physical measurements: orbital period, transit depth and duration, inferred planet radius, equilibrium temperature, insolation, signal-to-noise, impact parameter, and the host star’s temperature, gravity, and radius.</p> <p>Merging TESS in was the part I underestimated. TESS reports period, duration, depth, planet radius, temperatures, and host-star properties, but it does not report Kepler’s model SNR or impact parameter. Rather than drop those two features or those rows, I left them as NaN for TESS and median-imputed them alongside everything else. The two missions also use different disposition codes, so I wrote a small map (CP and KP to confirmed, FP and FA to false positive, PC and APC to candidate) onto the same schema.</p> <h2 id="the-model-and-what-it-learned">The model and what it learned</h2> <p>The shipped classifier is a gradient-boosting model in scikit-learn, 400 trees at depth 3, exported to ONNX so it runs client-side with ONNX Runtime Web. No server is needed to make a prediction. On a held-out 20% test split it reaches 0.902 accuracy, 0.963 ROC-AUC, and 0.881 F1, with 5-fold cross-validated AUC of 0.958. I also fit a logistic regression on standardized features as an interpretable surrogate; it is weaker (0.858 AUC) but it drives a “why” panel that shows which measurements pushed each call.</p> <p>What I find convincing is that the model still works without the answer key. It learns the actual physics: an inferred radius of tens of Earth-radii is star-sized and means false positive, a grazing transit with high impact parameter is suspect, low signal-to-noise often means noise. You can drag the planet-radius slider up and watch a confirmed verdict flip.</p> <p>The AI tutor (GPT-4o-mini, behind a tiny Python proxy that keeps the key server-side) is deliberately narrow. It is given the model’s verdict and the real numbers and told to explain the transit method, never to re-decide. The right role for an LLM here is to explain a trustworthy model, not to be the model.</p> <p>Code: <a href="https://github.com/garyzhang1006/exos">github.com/garyzhang1006/exos</a></p>]]></content><author><name></name></author><category term="projects"/><category term="machine-learning"/><category term="astronomy"/><category term="onnx"/><summary type="html"><![CDATA[an ONNX exoplanet classifier and what the light-curve data taught me]]></summary></entry></feed>